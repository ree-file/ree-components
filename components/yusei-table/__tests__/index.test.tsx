import React from 'react';
import { render } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';
import YuseiTable from '../index';

describe('YuseiTable', () => {
  it('renders without error', () => {});
});
